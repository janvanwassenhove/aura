# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.env
.env.local
*.egg-info/
dist/
build/
# U174: maar de desktop-iconen MOETEN mee — dit ontbrak en daardoor had
# elke CI-build geen icoonbestand en vielen alle releases stil terug op
# het default Electron-icoon ("application icon is not set").
!apps/desktop/build/
!apps/desktop/build/**
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Node / Vue
node_modules/
dist/
.nuxt/
.output/
*.local

# Docker
.docker/

# Editors
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets
*.pem
*.key
secrets/
*.env.bak

# Runtime data (memory DB, encrypted knowledge store — never commit)
data/

# SQLite
*.db
*.sqlite3

# Logs
*.log
logs/

# uv
.uv/


.github/apm
# Windows crash dumps
*.stackdump
skills/.metrics/

# U183: skills are the owner's PERSONAL routines (playlists, habits, names).
# The vault lives in userData at runtime; only the explainer stays in git.
skills/*.md
!skills/README.md

# U207: saved presentation scenarios are the owner's talk content.
scenarios/*.yaml
